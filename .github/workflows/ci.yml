name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # The go.mod minimum and the current release: the minimum is a promise,
        # so it gets tested rather than assumed.
        go: ['1.24', 'stable']
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.go }}
          # There are no dependencies, so there is no go.sum to key a cache on.
          cache: false
      - run: go vet ./...
      - run: go test -race -count=2 ./...

  # The cache is pure Go, but expiry leans on the wall clock and eviction on map
  # iteration order, so the other platforms are worth a run.
  platforms:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: stable
          cache: false
      - run: go test -race -count=1 ./...

  # The comparison benchmarks pull in the caches they compare against, so they
  # live in their own module and are only built and smoke-run here. Their numbers
  # come from a quiet machine, not from a shared runner.
  benchmarks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: stable
      # One run covers both the guard test that every cache here is configured to
      # actually store things, and a token iteration of each benchmark, which is
      # what keeps them compiling.
      - run: go test -bench . -benchtime 10x ./...
        working-directory: benchmarks

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: stable
          cache: false
      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.2
      - name: gofmt
        run: test -z "$(gofmt -l .)" || { gofmt -l .; exit 1; }
